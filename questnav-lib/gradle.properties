# QuestNavLib Build Configuration

# Version Configuration (can be overridden via -P flags)
version=2027-1.0.0-dev
versionType=dev
frcYear=2027_alpha7
wpilibVersion=2027.0.0-alpha-7

# Build Configuration
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true

# Publishing Configuration (set via command line or CI)
mavenRepoUrl=https://maven.questnav.gg
# mavenUsername=
# mavenPassword=

# Java Configuration
java.incremental=true